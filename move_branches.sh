#!/bin/bash

# Move branches with a specific prefix to a folder
#git branch | grep '^ABC-' | xargs -I branch_name git branch -m branch_name features/branch_name


echo "Shell script execution test successful!"
